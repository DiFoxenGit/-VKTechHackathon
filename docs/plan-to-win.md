# Что осталось сделать, чтобы выиграть

Аудит проведён 22 сентября 2026 на коммите `dece47c`. Проверялось не по памяти: перечитано
ТЗ целиком, прогнан регресс на четырёх шаблонах, сделана живая генерация через сервис,
проверены репозиторий, CI, сервер и интерфейс.

У каждой задачи рядом с описанием лежит готовый промпт — его можно отдать целиком, не
пересказывая проект.

## Где мы сейчас

| Что | Факт |
|---|---|
| Живая генерация | 12 слайдов за **100 секунд** (лимит ТЗ — 5 минут), ноль ошибок аудита |
| Нативность | 77–83 редактируемых объекта на колоду, слайдов-картинок нет |
| Визуализации в колоде | process, bar, timeline, table — в одной колоде сразу четыре вида |
| Регресс, 4 шаблона × 3 варианта | 0 ошибок; предупреждений 1–4 на шаблонах кейса, 15 на незнакомом ЛЦТ2026 |
| Приложение 1 | все 24 пункта закрыты детерминированными проверками, плюс 12 сверх списка |
| Тесты | 130 сценариев, CI зелёный: Tests (pytest, frontend, регресс) → Deploy |
| Сервер | тот же коммит, что в master, деплой из CI по git |

---

# P0. Без этого решение не засчитают

## P0-1. Открыть репозиторий экспертам

Репозиторий приватный. Требование обоих этапов сдачи — «репозиторий с открытым доступом для
экспертов». Делает DiFoxen: настройки репозитория → сделать публичным либо добавить экспертов
в коллабораторы.

## P0-2. Видео-демо до 7 минут

Записи нет. Требование промежуточного этапа: запись экрана без монтажных склеек или живой
прогон. Структура питча из ТЗ: проблема, подход, архитектура пайплайна, живое демо,
архитектура аудита, выводы.

Сценарий, который стоит показать: незнакомый шаблон (ЛЦТ2026) загружается в интерфейс →
бриф и материалы → структура на экране «Сначала — главное» → три варианта → редактор,
правка слайда → панель «Проверка слайдов» с подсветкой находок → «Исправить» → скачанный
PPTX открывается в PowerPoint и там редактируется.

## P0-3. Пересобрать девять эталонных колод

**Почему сейчас.** Набор в `samples/` собран 21 сентября в 12:38, а большая работа по
визуальным ассетам (иконки из шаблона, иллюстрации, фактоиды, замена значков-образцов)
влилась позже, в 15:43. Жюри промежуточного этапа увидит колоды **до** главного улучшения
качества. Плюс с тех пор закрыты проверки файла и подписи диаграмм.

**Что требует ТЗ.** «Результат демонстрации сервиса — три варианта презентации,
сгенерированные на одном контенте на трёх разных шаблонах (всего 9 вариантов презентаций)».
Контент один и тот же, шаблоны три разных, варианты три.

**Как собирается.** `tools/make_samples.py` загружает `samples/source/материал-инициатива.md`
контент-пакетом, берёт три шаблона с сервера, запускает одну задачу на шаблон (в каждой
сервис верстает сразу три варианта), дожидается и складывает в `samples/` девять `.pptx`,
девять `.pdf` и `report.json` с числами по каждой колоде.

```
Задача: пересобрать набор samples/ на текущем master.

Контекст: девять колод в samples/ собраны 21 сентября до влития визуальных ассетов
(PR #25) и проверок файла (PR #24), то есть показывают старое качество. ТЗ требует три
варианта на трёх шаблонах на одном контенте — всего 9.

Сделай:
1. Проверь, что сервер на текущем master: ssh ... 'cd ~/vk-designer && git log --oneline -1'.
2. python tools/make_samples.py http://158.160.225.35
3. Обнови таблицу в samples/README.md числами из свежего samples/report.json, допиши дату и
   коммит сборки.
4. Посмотри глазами по одной колоде каждого шаблона: обложка, слайд с диаграммой, слайд с
   иконками, финал. Увидишь брак — не заливай, опиши, что не так.
5. Одним PR: samples/* и README.

Готово, когда: 9 колод собраны на одном коммите, в каждой 10–15 слайдов, ошибок аудита 0,
время каждой задачи меньше 5 минут, в колодах видны иконки по смыслу, иллюстрации и
фактоиды, в README свежая таблица.
```

---

# P1. Пункты ТЗ, которые сейчас видны как пробел

## P1-4. Версия воркфлоу и агентов в интерфейсе

Пункт 4 ТЗ — «версионирование скиллов и агентов». Бэкенд отдаёт всё по `GET /api/v1/workflow`,
но после возврата прототипа бейдж из интерфейса пропал: жюри этого не увидит. Оценка — 1 час.

```
Задача: показать в интерфейсе версию воркфлоу и состав агентов.

Контекст: ТЗ, пункт 4 — «версионирование скиллов и агентов». Бэкенд отдаёт всё по
GET /api/v1/workflow: version, agents (файлы промптов), sha256 каждого файла, changelog с
причиной изменения. В клиенте метод уже есть — api.workflow() в frontend/src/services/api.ts.
Раньше бейдж был, но при возврате прототипа пропал.

Сделай: в шапке рядом с кнопкой «Как это работает» — чип «воркфлоу X.Y.Z», по клику
раскрывается карточка: список агентов с файлами и первыми 8 символами sha256, последняя
запись changelog с причиной. Карточка закрывается кликом мимо и клавишей Escape. Стиль — как
у остального интерфейса, без тёмных плашек.

Готово, когда: версия видна на главной без переходов, карточка открывается и закрывается,
npx tsc --noEmit и npm run build проходят, проверено в браузере на стенде.
```

## P1-5. Подсветка находок аудита в превью

Пункт 6 ТЗ — «система визуализирует найденные проблемы». Сервер умеет рисовать рамки вокруг
проблемных блоков, но интерфейс запрашивает превью с `highlight=false`. Оценка — 1 час.

```
Задача: показывать найденные аудитом проблемы прямо на превью слайда.

Контекст: ТЗ, пункт 6 — «система визуализирует найденные проблемы». Сервер это умеет:
GET /api/v1/presentations/{id}/slides/{index}/preview?highlight=true рисует рамки вокруг
проблемных блоков. Интерфейс сейчас запрашивает превью с highlight=false —
frontend/src/App.tsx, вызов presentationService.preview(project, activeSlide, false).

Сделай: включить подсветку по умолчанию и добавить в окно превью переключатель
«Показать замечания», чтобы можно было посмотреть слайд и без рамок. Рядом — счётчик находок
на этом слайде. При переключении перезапрашивать превью, старую blob-ссылку освобождать
(URL.revokeObjectURL).

Готово, когда: в превью видно рамки вокруг проблемных блоков, переключатель работает, утечки
blob-ссылок нет, проверено на колоде, где аудит что-то нашёл.
```

## P1-6. Контекстный аудит и проверка по изображению из интерфейса

Недетерминированная часть Приложения 1 работает на бэкенде, но кнопок в интерфейсе нет — на
защите её нечем показать, а ТЗ требует объяснить, какие проверки детерминированные, а какие
контекстуальные. Оценка — 2 часа.

```
Задача: запускать недетерминированные проверки из интерфейса.

Контекст: ТЗ требует показать на защите, какие проверки детерминированные, а какие
контекстуальные. Бэкенд умеет обе: POST /api/v1/presentations/{id}/audit?contextual=true
(текстовая проверка моделью) и ?visual=true (слайд рендерится в PNG и уходит мультимодальной
модели с 11 вопросами Приложения 1). В клиенте есть api.runAudit и
presentationService.recheck. Кнопок в интерфейсе нет.

Сделай: в панели «Проверка слайдов» две кнопки — «Проверить содержание» и «Проверить по
изображению». Пока идёт проверка — состояние загрузки и подпись, что это занимает около
минуты на восемь слайдов. Результат дописывается в тот же список находок; у контекстных
находок пометка, что их нашла модель, и что автоматического исправления у них нет.

Готово, когда: обе проверки запускаются из интерфейса, находки видны в общем списке с
пометкой происхождения, повторный запуск не дублирует список, проверено на стенде.
```

## P1-7. Обоснование оси различий трёх вариантов

ТЗ: «Ось различий команда определяет сама и обосновывает свой выбор в документации». Сейчас в
`ARCHITECTURE.md` об этом одна строка. Оценка — 1 час.

```
Задача: описать в ARCHITECTURE.md, чем отличаются три варианта вёрстки и почему выбрана
именно такая ось различий.

Контекст: ТЗ, пункт 5: «Ось различий команда определяет сама и обосновывает свой выбор в
документации. Варианты должны быть визуально различимы и при этом одинаково соответствовать
правилам шаблона». Сейчас в ARCHITECTURE.md об этом одна строка. Реальные различия заложены
в backend/designer/layout.py: classic — вертикальный рассказ, split — две колонки, focus —
один акцент крупным кеглем; плюс разные композиции для слайдов без визуализации (фактоиды,
список с иконками, текст с иллюстрацией) и разная доля площади под акцент.

Сделай: раздел «Три варианта вёрстки» — что именно меняется (композиция и плотность, а не
палитра и не гарнитура), почему выбрана эта ось (шаблон задаёт стиль, варьировать можно
только композицию, иначе нарушится дизайн-система), таблица «что делает каждый вариант» и
примеры страниц из samples/. Ссылки на функции layout.py, чтобы утверждения проверялись.

Готово, когда: раздел отвечает на вопрос жюри «почему именно эти три варианта» без устных
пояснений, и каждое утверждение подтверждается кодом или примером.
```

## P1-8. Актуализация gap-analysis

`docs/gap-analysis.md` отражает состояние до визуальных ассетов, проверок файла, экрана
структуры и возврата интерфейса. Оценка — 30 минут.

```
Задача: привести docs/gap-analysis.md в соответствие с кодом.

Контекст: документ отражает состояние до визуальных ассетов (PR #25), проверок файла
(PR #24), экрана структуры (PR #22) и возврата интерфейса (PR #19).

Сделай: обнови таблицу соответствия ТЗ, отметь закрытые пункты, перечисли оставшиеся со
ссылками на docs/plan-to-win.md. Уточни числа: тестов 130, регресс на 4 шаблонах, 24 из 24
пунктов Приложения 1.

Готово, когда: ни одно утверждение в документе не расходится с кодом и прогонами.
```

---

# P2. Качество результата — критерий «Эффективность решения»

## P2-9. Текст на фотофонах незнакомого шаблона

Регресс на ЛЦТ2026 даёт 12 находок `text_over_image` и 1 `branding_overlap` на колоду против
1–4 на шаблонах кейса: там почти каждая страница с фотофоном. Это прямо бьёт по критерию
«решение реально адаптируется к произвольному шаблону». Оценка — 3–4 часа.

```
Задача: убрать находки text_over_image на шаблоне ЛЦТ2026.

Контекст: регресс на незнакомом шаблоне даёт 12 находок text_over_image и 1
branding_overlap на колоду, тогда как на шаблонах кейса их 1–4. В ЛЦТ2026 почти каждая
страница — фотофон. Проверить: python tools/bench_templates.py
"../backend-audit/Презентация/ЛЦТ2026 Шаблон презентации.pptx" --outline
tools/regress_outline.json --source samples/source/материал-инициатива.md --render out/
--sheets out/ — и смотреть контакт-листы.

Смотри: layout.py — score_pattern (штрафы image_cover и bg_spread), выбор подложки
(needs_scrim, измеренные bg_luma и bg_spread), audit.py — правило text_over_image.

Сделай: при равном качестве предпочитать страницы со спокойным фоном; там, где фон всё-таки
пёстрый, подложка должна быть честной (по измеренному фону, а не белой плашкой поверх
дизайна). Правило аудита не ослаблять.

Готово, когда: на ЛЦТ2026 не больше 5 находок text_over_image на колоду, на шаблонах кейса
число находок не выросло, ошибок по-прежнему 0, контакт-листы выглядят не хуже.
```

## P2-10. Разреженные слайды

16 находок `fill_ratio` на 36 колод: слайд заполнен меньше четверти рабочей области, а
Приложение 1 требует 25–75%. Оценка — 2–3 часа.

```
Задача: уменьшить число находок fill_ratio.

Контекст: в регрессе 16 находок fill_ratio на 36 колод — слайд заполнен меньше четверти
рабочей области. Приложение 1 требует 25–75%.

Смотри: layout.py — grow_text (рост кегля по шкале шаблона), композиции для слайдов без
визуализации, center_content; generation.py — плотность тезисов, промпт outline.v3.txt.

Сделай: разобраться, что именно недозаполнено — слайды с одним-двумя короткими тезисами или
страницы шаблона с маленькой рабочей областью. Решение выбрать по факту: растить кегль,
менять композицию или просить модель давать больше содержания. Порог аудита не «подкручивать».

Готово, когда: fill_ratio не больше 5 находок на 36 колод, ошибок 0, слайды не выглядят
перегруженными (проверить по контакт-листам).
```

## P2-11. Размер мультимодальной модели

ТЗ разрешает модели до 35B. У `Qwen3.6-35B-A3B` активных параметров 3B, но карточка Hugging
Face показывает 36B общих — формально это можно прочитать против нас. Оценка — 30 минут.

```
Задача: снять риск по лимиту 35B.

Контекст: ТЗ разрешает модели с открытыми весами до 35B. Мы используем Qwen3.6-35B-A3B:
активных параметров 3B, но карточка Hugging Face показывает 36B общих.

Сделай: либо переключить DESIGNER_VLM_MODEL на Qwen3.6-27B (той же лицензии Apache 2.0) и
проверить, что аудит по картинке работает не хуже — прогнать
POST /presentations/{id}/audit?visual=true на готовой колоде и сравнить находки; либо
оставить и заранее подготовить в MODELS.md абзац с обоснованием, почему считать нужно по
активным параметрам.

Готово, когда: решение принято, MODELS.md говорит то же, что .env на сервере, и это
подтверждено прогоном.
```

## P2-12. Четыре браузера

ТЗ требует корректной работы в актуальной и предыдущей версиях Chrome, Firefox, Safari и
Яндекс Браузера на macOS и Windows. Проверяли только Chromium. Риски: единицы `cqw` (32
использования), селектор `:has()`, элемент `<dialog>`, `backdrop-filter`, `dvh`.
Оценка — 2 часа.

```
Задача: проверить интерфейс в Chrome, Firefox, Safari и Яндекс Браузере.

Контекст: ТЗ требует корректной работы в актуальной и предыдущей версиях четырёх браузеров
на macOS и Windows. Мы проверяли только Chromium. Риски: единицы cqw (32 использования в
стилях), селектор :has(), элемент dialog, backdrop-filter, dvh.

Сделай: пройти полный путь в каждом браузере — бриф, структура, три варианта, редактор,
превью, экспорт PPTX. Снять скриншоты. Что не работает — починить или заменить на
совместимое, не ломая вид в остальных.

Готово, когда: во всех четырёх браузерах путь проходится до скачанного файла, скриншоты
приложены к PR, найденные различия описаны в docs/worklog.
```

## P2-13. Блок вплотную к декору шаблона

3 находки `branding_overlap` на 36 колод. Мелочь, но видна глазу. Оценка — 1 час.

```
Задача: убрать оставшиеся находки branding_overlap.

Контекст: в регрессе 3 находки на 36 колод — блок подходит вплотную к декоративному элементу
шаблона. Смотри layout.py: dodge_decor (сдвиг блока ниже декора) и score_pattern (штраф
branding_in_band), audit.py — правило branding_overlap.

Сделай: разобрать каждый случай по контакт-листу, понять, почему dodge_decor не сработал
(некуда двигать, декор крупный, блок из шаблона), и починить причину.

Готово, когда: branding_overlap нет ни в одной из 36 колод регресса, остальные находки не
выросли.
```

## P2-14. PR #1: разобрать наработки о повторах и выдуманных графиках

**Что это.** PR #1 от kiritomp3 (17 сентября, +794/−17 в 9 файлах) чинил реальные дефекты:
одна и та же фраза заголовком и тезисом на трёх слайдах, заголовки дословно из названий
шагов каркаса, «график» по порядковым номерам этапов, тезис кеглем крупнее заголовка,
растянутая таблица.

**Почему висит.** Ветка не обновлялась пять дней, а master с тех пор переписан несколько раз:
пробное слияние даёт **конфликты в шести файлах** — `audit.py`, `layout.py`, `generation.py`,
`exporting.py`, `manifest.json`, `test_pipeline.py`. Тесты в PR ни разу не запускались (автор
сам отметил это в описании). Проверок из PR в master нет: `repeated_content`,
`chart_without_data`, `body_over_title`, `repair_outline` отсутствуют.

**Что делать.** Не вливать как есть. Перенести идеи на текущий master отдельными маленькими
PR с тестами, а PR #1 закрыть с комментарием и ссылками на преемников.

```
Задача: перенести полезное из PR #1 на текущий master.

Контекст: PR #1 «Колода из короткого брифа» висит с 17 сентября, ветка
fix/stretched-brief-quality. Пробное слияние даёт конфликты в шести файлах, тесты автор не
запускал. Но дефекты, которые он чинил, настоящие, и в master этих проверок нет:
- repeated_content — один и тот же тезис или заголовок на нескольких слайдах;
- chart_without_data — «график» построен по порядковым номерам, а не по данным из материалов;
- body_over_title — кегль тезиса больше кегля заголовка.

Сделай: три отдельных PR, каждый с тестом, поверх текущего master. Идеи бери из diff
PR #1 (git diff master...origin/fix/stretched-brief-quality), но код пиши под сегодняшнюю
архитектуру: аудит и вёрстка с тех пор изменились. После вливания закрой PR #1 комментарием
со ссылками на новые PR.

Готово, когда: три проверки работают и покрыты тестами, регресс на четырёх шаблонах не
показывает новых находок, PR #1 закрыт.
```

---

# P3. К финалу, если проходим в топ-10

## P3-15. Генерация изображений в слайде (задача со звёздочкой)

Пункт 3.1 ТЗ, обязателен для топ-10: модель text-to-image до 20B, открытые веса, Apache 2.0
или MIT.

```
Задача: подключить генерацию изображений для слайдов.

Контекст: ТЗ, пункт 3.1 — для команд, прошедших отбор: модель класса text-to-image до 20B,
открытые веса, лицензия Apache 2.0 или MIT. У нас уже есть слой картинок: assets.py
подбирает иллюстрации из контент-пакета, шаблона и встроенного набора, layout.py умеет режим
picture, аудит проверяет растяжение картинки.

Сделай: провайдер text-to-image по тем же правилам, что LLM и VLM — адрес и модель из
переменных окружения, ключ на сервере. Промпт строится из заголовка и тезисов слайда плюс
палитра шаблона, чтобы картинка не выбивалась из стиля. Картинка вставляется в слот без
растяжения, кадрируется по рамке. Если провайдер недоступен, слайд собирается как сейчас —
подобранной иллюстрацией. Промпт вынести отдельным файлом в designer/prompts, добавить в
manifest.json и changelog.

Готово, когда: картинка появляется в колоде по запросу, стиль совпадает с шаблоном, на слайде
нет постороннего текста с картинки, аудит не находит растяжения, есть тесты на недоступность
провайдера и на вставку без искажения пропорций.
```

## P3-16. Переезд на инференс VK

Для топ-10 использование инференса VK обязательно (Qwen 3.8 27b).

```
Задача: подготовить переключение на инференс VK (Qwen 3.8 27b).

Контекст: для топ-10 использование инференса VK обязательно. У нас провайдер задаётся
переменными DESIGNER_LLM_BASE_URL, DESIGNER_LLM_MODEL, DESIGNER_LLM_API_KEY, клиент —
OpenAI-совместимый (designer/generation.py).

Сделай: проверить совместимость их API с нашим клиентом на тестовом ключе: формат
/chat/completions, имена полей, лимиты, таймауты. Если есть отличия — спрятать их за тем же
интерфейсом, не разнося по коду. Описать переключение в MODELS.md и docs/deploy.md.

Готово, когда: одна строка в .env переключает сервис на VK, колода собирается целиком, время
укладывается в 5 минут, документация описывает обе конфигурации.
```

## P3-17. Очередь задач

Сейчас лимит — 20 активных или ожидающих задач, дальше 429. Для демо под нагрузкой нужна
внешняя очередь и несколько воркеров; сегодня compose запускает один процесс.

---

# Порядок работ

1. Сегодня: P1 целиком (4–8) — прямые пункты ТЗ, делается за рабочий день.
2. Сразу после: пересборка эталонов (P0-3) на обновлённом коде.
3. Дальше: P2-9 и P2-10 — они влияют на то, как колоды выглядят у жюри.
4. Параллельно силами команды: репозиторий и видео.
5. К финалу: P3.
