# Архитектура

## Границы слоёв

| Модуль | Ответственность |
|---|---|
| `designer/app.py` | HTTP: загрузки, валидация, CORS, общий токен, шаблоны, материалы, задачи, ревизии, экспорт. Схемы запросов — `models.py`. `DESIGNER_DOCS=0` снимает Swagger, ReDoc и `openapi.json` с публичного сервера. |
| `designer/storage.py` | SQLite с атомарной записью JSON, исходные PPTX и артефакты на диске. UUID не зависит от имени файла, SQL параметризован, пользовательские имена не превращаются в пути. |
| `designer/parsing.py` | Разбор шаблона в дизайн-систему. Ни одного обращения к модели. |
| `designer/generation.py` | Бриф и материалы → валидированная `Outline`; контекстуальный аудит; аудит по картинке; сокращение перегруженных слайдов. |
| `designer/language.py` | Проверки письменности: язык колоды, подписи не на том языке, допустимая латиница (KPI, API, Q1). Общие для планирования и аудита. |
| `designer/layout.py` | Общая структура + шаблон → три геометрические модели. |
| `designer/visuals.py` | Нативные схемы и пиктограммы: процесс, цикл, пирамида, таймлайн, сравнение; запасные иконки как композиции автофигур. |
| `designer/assets.py` | Визуальные ассеты: сбор картинок из шаблона и контент-пакетов, классификация (иконка, иллюстрация, фото), подписи мультимодальной моделью, подбор по смыслу тезиса, SVG → нативные фигуры, перекраска растровых иконок. Встроенный набор — `designer/assets/pack/`. |
| `designer/audit.py` | 38 кодов находок: детерминированные правила, проверки готового файла, контекстуальный аудит текста, аудит по изображению слайда, применение исправлений. |
| `designer/exporting.py` | Нативный PPTX, PDF через LibreOffice, HTML из SVG-страниц, рендер слайдов в PNG, измерение фона. |

## Что извлекается из шаблона

`parsing.py` строит паспорт дизайн-системы, а не список картинок:

- **Токены**: гарнитуры, типографическая шкала, палитра, цвета темы.
- **Типографика по умолчанию**: `p:txStyles` мастера и `a:lstStyle` каждой рамки — кегль, начертание, выравнивание и отступы берутся оттуда, а не из констант.
- **Геометрия**: рабочая область, внешние поля, вертикальные направляющие.
- **Паттерны**: каждый слайд-образец с его текстовыми слотами, ролью (`cover`, `agenda`, `section`, `team`, `closing`, `content`), площадью декора, фоном и рамками брендинга, которые обязаны пережить клонирование.
- **Фон**: объявленный в XML (заливка, градиент, картинка) и — отдельно — **измеренный**: страница рендерится в маленький PNG, считается яркость и разброс. XML часто «лжёт» (тёмное фото поверх белой заливки), а по измерению видно, нужна ли подложка под текст.

- **Картинки**: иконки, иллюстрации и фото со страниц-образцов складываются в библиотеку ассетов шаблона; повторяющийся брендинг и декор (полосы, фоны, логотипы) не берутся. Значки над карточками запоминаются как **места под иконку** (`icon_slots`) вместе с цветом.

Слабый шаблон — это нормальный вход: если ролей не распозналось, слотов мало или страницы захламлены декором, набор кандидатов расширяется правилами послабления, а композиция опирается на рабочую область и шкалу.

## Пайплайн

```
шаблон ─┐
         ├─→ паспорт дизайн-системы ─┐
материалы┘                            ├─→ Outline ─→ classic / split / focus ─→ аудит ─→ нативный PPTX ─→ правки → новая ревизия ─→ PDF / HTML
                                     каркас повествования
```

1. **Материалы → контекст.** Тексты бьются на фрагменты, отбираются по релевантности брифу, бюджет — 24 000 символов (`DESIGNER_CONTEXT_CHARS`).
2. **Каркас повествования.** `prompts/narratives.v1.json` задаёт последовательность смыслов для назначения колоды (проект, фича, продукт, инициатива). Слайд о команде появляется только там, где он уместен и в материалах есть о ком рассказывать.
3. **План до вёрстки.** Модель возвращает структуру: заголовки, тезисы, визуализации, ссылки на источники. Проверяется число слайдов, ссылки на реальные материалы, единый язык подписей, отсутствие выдуманных чисел, дубли заголовков.
4. **Баланс по вместимости.** `slide_capacity` считает, сколько символов вмещает контентная область **этого** шаблона. Перегруженные слайды уходят агенту `condense`; если модель недоступна, текст подрезается детерминированно.
5. **Композиция.** Для каждого слайда выбирается паттерн шаблона (см. ниже), из него берутся слоты, кегли, выравнивание и вертикальный ритм. Ряд одинаковых рамок распознаётся как карточная сетка, и тезисы раскладываются по карточкам.
6. **Аудит в конвейере** — до того, как пользователь увидит результат.
7. **Экспорт.** Исходный PPTX клонируется с мастерами, макетами и рисунками; текст, таблицы, диаграммы и фигуры добавляются нативными объектами. `verify_pptx` открывает результат и подтверждает, что слайды состоят из редактируемых объектов.

Одна Outline применяется ко всем трём вариантам без изменения фактов. Различаются положение блоков, колоночность и размер области акцента; палитра, гарнитура и шкала — общие, из исходного файла.

## Картинки на слайде

Слайд с тезисами без визуализации получает одну из композиций — выбор объясним и детерминирован:

- **Фактоиды**: в двух-четырёх тезисах есть весомое число («с 186 до 4 минут», «2 из 68», «23%»). Число берётся из текста дословно и набирается верхней ступенью шкалы шаблона, тезис — подписью под ним. classic — ряд плиток, split — сетка, focus — одна цифра героем.
- **Список с иконками** (classic): к тезисам нашлись иконки по корням слов. Иконки одного набора на слайде — пакет пользователя, шаблон или встроенный; неподобранный тезис получает нейтральный знак того же набора. Если у выбранной страницы шаблона есть карточки с местами под значки, берётся она.
- **Текст и иллюстрация** (split, focus): подходящая иллюстрация или фото по заголовку и тезисам; картинка вписывается без растяжения, фото кадрируется.

На карточных страницах шаблона значки-образцы («лампочка» над каждым тезисом) заменяются иконками по смыслу — в ту же рамку и в тот же цвет. Мелкие образцы страницы (стрелки, точки, соединители) на слайдах со своей композицией и с диаграммой не переносятся.

Источники картинок по приоритету: контент-пакет → шаблон → встроенный набор. Картинки шаблона без подписи не подбираются: их подписывает агент `assets` (мультимодальная модель, 16 картинок на вызов), подписи хранятся в записи шаблона и переживают переразбор.

## Три варианта вёрстки

Ось различий — **композиция и распределение визуальной плотности**: порядок чтения,
колонки, размер и положение акцента. Это три способа предъявить одну Outline,
а не три независимых ответа модели. Палитра, гарнитуры, типографическая шкала,
декор и рабочие области приходят из одного шаблона. Конкретный кегль может
меняться в пределах выбранной шкалы и вместимости рамки.

Почему выбрана эта ось: шаблон уже задаёт фирменный стиль. Смена палитры или
гарнитуры превратила бы выбор варианта в выбор другой дизайн-системы. Композиция
позволяет предложить полезный выбор внутри неё: последовательное объяснение,
сопоставление текста с данными или выделение главного решения. Эти три режима
соответствуют разным задачам выступающего, сохраняя общий исходный материал.
Это проектное обоснование; пользовательское исследование предпочтений не проводилось.

| Вариант | Текст и визуализация | Тезисы без заданной визуализации | Зачем выбирать |
|---|---|---|---|
| `classic` | Обычно тезисы сверху, визуализация ниже на всю ширину. | Список или карточки шаблона; при подходящих данных — ряд фактоидов, список с иконками либо иллюстрация рядом с коротким тезисом. | Последовательный рассказ: сначала объяснение, затем подтверждение. |
| `split` | Текст слева, визуализация справа, колонки равной ширины за вычетом промежутка. | От четырёх тезисов — две колонки; фактоиды — сетка в две колонки; подходящая иллюстрация — справа от текста. | Одновременное чтение объяснения и данных, сопоставление блоков. |
| `focus` | Визуализация сверху, поддерживающие тезисы снизу. | Первый тезис получает отдельный акцентный блок, остальные сохраняются ниже; у фактоидов крупно выделяется первое число, остальные тезисы идут текстом. | Зафиксировать одну главную мысль или показатель, оставив подтверждения на слайде. |

Проверяемая реализация в [layout.py](backend/designer/layout.py):

- `compose` задаёт ветки `split`, `has_visual`, `focus` и общую геометрию.
  В обычном текстовом `focus` доля акцентной рамки вычисляется по объёму текста
  в диапазоне `LEAD_SHARE_RANGE` (34–52% рабочей высоты); единственный тезис
  получает всю доступную область. Это доли рамок, а не гарантия заполненности.
- `stat_elements` раскладывает одинаковые числовые тезисы в ряд, сетку или
  один крупный фактоид. Предельный кегль числа ограничен 13% высоты слайда
  для `classic`/`split` и 20% для `focus`, с подбором по шкале и ширине текста.
- `slide_mode` выбирает фактоиды по содержанию; для `classic` подбирает иконки
  к списку из 2–5 тезисов; иллюстрация возможна при объёме до 320 символов
  и не более 1/4/2 тезисов для `classic`/`split`/`focus` соответственно.
  Нужен подходящий ассет; обложка и финал исключены из этого выбора.
- В ветке `picture` функции `compose` текст занимает 56% ширины без промежутка
  для `classic`/`split`, 52% для `focus`; оставшаяся часть отдана иллюстрации.
  `picture_box` сохраняет пропорции иллюстрации, фотография использует `cover`.
- `grow_text`, `fit_text` и `focus_accent` регулируют кегль и читаемость;
  `choose_pattern`, `decor_free_band` и `dodge_decor` учитывают рамки и декор.

Различия не принудительны на каждой странице. При коротком списке `split`
оставляет один блок; узкая специальная область шаблона может исключить две
колонки. Если диаграмме и тексту тесно по высоте, `classic` и `focus` тоже
переходят к колонкам. Обложка, финал и карточки учитывают ограничения шаблона.
Соответствие дизайну и читаемость важнее искусственного различия одной страницы;
ко всем вариантам применяется один и тот же аудит, его пороги не зависят от варианта.

### Примеры из сохранённых колод

Сравнивать нужно одинаковые номера страниц внутри одного шаблона. Ниже —
набор VK Tech из `samples/`, сборка `30a9d9c` от 22 сентября 2026
(происхождение и результаты аудита — [samples/README.md](samples/README.md)).

| Страница и наблюдаемое различие | Classic | Split | Focus |
|---|---|---|---|
| 2, «Контекст и возможность: ручная сборка 42 колод»: четыре фактоида в ряд / сетка 2 × 2 / акцент на «186 мин», остальные тезисы ниже. | [PDF, стр. 2](samples/VK_Tech_шаблон__classic.pdf#page=2) | [PDF, стр. 2](samples/VK_Tech_шаблон__split.pdf#page=2) | [PDF, стр. 2](samples/VK_Tech_шаблон__focus.pdf#page=2) |
| 12, «Что мы просим решить: запуск на всю компанию»: общий список в classic и split; отдельное решение крупным блоком и поддержка ниже в focus. | [PDF, стр. 12](samples/VK_Tech_шаблон__classic.pdf#page=12) | [PDF, стр. 12](samples/VK_Tech_шаблон__split.pdf#page=12) | [PDF, стр. 12](samples/VK_Tech_шаблон__focus.pdf#page=12) |

Редактируемые источники для проверки объектов:
[classic PPTX](samples/VK_Tech_шаблон__classic.pptx),
[split PPTX](samples/VK_Tech_шаблон__split.pptx),
[focus PPTX](samples/VK_Tech_шаблон__focus.pptx).
Примеры показывают сохранённую сборку, а функции выше описывают текущие правила;
они не утверждают, что любая будущая генерация выберет те же паттерны.

## Выбор паттерна

Каждому слайду сопоставляется страница шаблона, и выбор объясним вслух:

- **Роль против назначения**: обложка получает страницу с ролью `cover`, финал — `closing`, контентный слайд теряет очки за обложку, оглавление и визитку команды.
- **Декор в рабочей полосе** штрафуется с коэффициентом 5: дешевле взять другую страницу, чем воевать с косой линией геометрией.
- **Фон**: страница с фотографией во весь слайд — раздел, а не место для текста.
- **Карточная сетка** под число тезисов даёт бонус, несовпадение — штраф.
- **Повтор** соседней страницы и каждое следующее использование дорожают, поэтому колода расходится по страницам, которые шаблон реально предлагает.

## Читаемость

Цвет текста выбирается из палитры шаблона по контрасту к **измеренному** фону один раз, и вёрстка, экспорт и аудит используют одно и то же решение. Если фон тёмный и пёстрый, под текст кладётся полупрозрачная подложка цвета фона — не белый прямоугольник поверх дизайна.

## Аудит

Три уровня, все внутри конвейера:

- **Детерминированный** (`audit.py`): выход за поля, пересечения, контраст, заполненность, кегль, длина заголовка, осиротевшие блоки, наезд на брендинг шаблона, соответствие чисел источникам. Элементы, пришедшие из шаблона (`from_template`), из части проверок исключены: их поставил дизайнер.
- **Контекстуальный** (текст): соответствие брифу, логика, тональность, поддержка чисел материалами.
- **По изображению**: слайд рендерится в PNG и уходит мультимодальной модели с вопросами «да/нет» о композиции, читаемости и соответствии шаблону.

Исправления применяются только по выбранным ID; после изменения проверки пересчитываются, неустранённая проблема остаётся в отчёте.

## Версионирование

`designer/prompts/manifest.json` хранит версию воркфлоу, файлы агентов и changelog с причиной каждого изменения. В задачу и презентацию пишется снимок: версия, sha256 каждого промпта, идентификатор модели. Любую колоду можно связать с тем, чем она сделана.

## Выполнение

Две одновременные генерации, максимум 20 активных или ожидающих задач. Обращения к модели асинхронные, тяжёлый экспорт PPTX — в потоке. Таймаут генерации 300 секунд после входа в слот; синхронный экспорт в потоке нельзя остановить таймаутом Python, поэтому предел не является жёсткой гарантией.

Задачи persistent по статусам, но не resumable: при перезапуске незавершённые помечаются ошибкой. Продакшн с несколькими процессами потребует внешней очереди и распределённых блокировок; текущий compose запускает один worker.

Ревизии неизменяемы на диске (`r1.pptx`, `r2.pptx`…) и пишутся после успешного экспорта. `revision` в PATCH и в применении исправлений даёт оптимистическую блокировку (409 при конфликте). PDF и HTML собираются во временном профиле LibreOffice и кешируются по ревизии.
